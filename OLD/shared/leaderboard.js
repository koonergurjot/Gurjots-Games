(function(){function key(g,s){return s?`leaderboard:${g}:${s}`:`leaderboard:${g}`;}function submitScore(g,score,seed){try{const k=key(g,seed);const arr=JSON.parse(localStorage.getItem(k)||'[]');arr.push({score:Number(score),seed});arr.sort((a,b)=>b.score-a.score);localStorage.setItem(k,JSON.stringify(arr.slice(0,5)));}catch{}}function getTopScores(g,seed,limit=5){try{const arr=JSON.parse(localStorage.getItem(key(g,seed))||'[]');return arr.sort((a,b)=>b.score-a.score).slice(0,limit);}catch{return[]}}window.LB=Object.assign(window.LB||{},{submitScore,getTopScores});})();
