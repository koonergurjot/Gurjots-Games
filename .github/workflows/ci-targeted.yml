name: Game Doctor (Targeted)

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  game-doctor:
    name: Game Doctor
    uses: ./.github/workflows/game-doctor.yml
    with:
      mode: targeted
    secrets: inherit
