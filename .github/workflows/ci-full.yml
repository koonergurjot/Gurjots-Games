name: Game Doctor (Full)

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  game-doctor:
    name: Game Doctor
    uses: ./.github/workflows/game-doctor.yml
    with:
      mode: full
    secrets: inherit
